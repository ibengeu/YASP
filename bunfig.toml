# Bun workspace configuration
# https://bun.sh/docs/install/workspaces

[install]
# In CI, use --frozen-lockfile flag instead of setting this here
# so local development can still update the lockfile freely
frozenLockfile = false
